while(x > 0) 
    x = x / 2;

do
    x = x / 2;
while(x > 0);

for(con; s>5; a++) 
    foo = 1;

if(cond)
    doThis + 4;
else
    doThat - 3;

switch(a)
{
    case 1: abCS;
    case 5: cd+Ke;
    default: efbs;
}